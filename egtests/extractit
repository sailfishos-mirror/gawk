:
#set -xv

echo "* Getting extract.awk in $s/it from the Italian gawk.texi"
cd $s
if [ ! -d "it" ]
then
	mkdir it
else
	rm -r it
	mkdir it
fi
cd it
cp $docit/gawk.texi gawktexi.it
gawk -f $b/extractit.awk gawktexi.it
cat $s/it/eg/prog/extract.awk $s/it/eg/lib/join.awk >$s/extract.it
	diff    $b/extractit.awk $s/extract.it
if [ "$?" -ne "0" ]
then
	echo vimdiff $b/extractit.awk $s/extract.it
	echo Extractit: the extract.awk script is different from the one in the book
fi
