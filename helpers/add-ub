#! /bin/sh

for i in . support extension
do
	sed -e '/CFLAGS =/s/$/ -fsanitize=undefined -fno-omit-frame-pointer/' \
		< $i/Makefile > foo
	mv foo $i/Makefile
	sed -e '/LDFLAGS =/s/$/ -fsanitize=undefined -fno-omit-frame-pointer/' \
		< $i/Makefile > foo
	mv foo $i/Makefile
	echo $i/Makefile
done
