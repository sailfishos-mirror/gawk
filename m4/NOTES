KEEP
----
dirfd.m4 needed by extension gawkfts.h.

gettext.m4 needs
- intlmacosx.m4 (can do without it, but with it result is smarter)

progtest.m4 - a little smarter than standard, so keep it.

triplet-transformation.m4 - we need GAWK_CANONICAL_HOST macro.

REMOVE
------
can remove extern-inline.m4, fcntl-o.m4, intdiv0.m4, c-bool.m4.

can remove intl.m4, codeset.m4 and iconv.m4, and intldir.m4.

Can remove lcmessage.m4, intmax.m4, intmax_t.m4, inttypes-pri.m4.
inttypes_h.m4, inttypes.m4, uintmax_t.m4
ax_check_compile_flag.m4 (after fixing pma.m4)

Can remove lock.m4, ltoptions.m4, ltsugar.m4, lt~obsolete.m4,
threadlib.m4, visibility.m4, xsize.m4.

Can remove longdouble.m4, longlong.m4, posix-printf.m4,
signed.m4, wchar_t.m4, and wint_t.m4.

CONSIDER
--------
Restore iconv.m4; it's a smarter version than standard.
